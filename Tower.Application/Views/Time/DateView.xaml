<UserControl x:Class="Tower.Application.Views.Time.DateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             DataContext="{Binding Time, Source={StaticResource Locator}}"
             d:DesignHeight="212" d:DesignWidth="800">
    <Viewbox Stretch="Uniform">
        <Label Foreground="#FFA4A4A4" HorizontalAlignment="Center" FontSize="20" Content="{Binding TimeService.DateText}" Padding="0">
            <Label.Effect>
                <DropShadowEffect ShadowDepth="0" Color="Black" Opacity="1" BlurRadius="6" />
            </Label.Effect>
        </Label>
    </Viewbox>
</UserControl>
