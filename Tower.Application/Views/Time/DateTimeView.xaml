<UserControl x:Class="Tower.Application.Views.Time.DateTimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             DataContext="{Binding Time, Source={StaticResource Locator}}"
             d:DesignHeight="300" d:DesignWidth="800">
    <Viewbox Stretch="Uniform">
        <StackPanel Orientation="Vertical">
            <Label Foreground="#FFFFFFFF" HorizontalAlignment="Center" FontSize="30" Content="{Binding TimeService.TimeText}" Padding="0" />
            <Label Foreground="#FFA4A4A4" HorizontalAlignment="Center" FontSize="20" Content="{Binding TimeService.DateText}" Padding="0" Margin="0,-6,0,0"/>
        </StackPanel>
    </Viewbox>
</UserControl>
